# 個人別 作業達成度と改善分析テンプレート v6 — 修正内容

v5 で確認されたバグの修正と仕上げを行ったものです。修正版は
`個人別_作業達成度と改善分析_v6.xlsx` です。

## 修正したバグ

### 1. ④改善優先順位の 9 位以下に「0」が並ぶ（①グラフにも波及）
順位索引（集計用シート AA/AB 列）が人数を超えた順位で「最初の空行」を指し、
INDEX が空セル参照で 0 を返していました。有効人数を超えた順位は空欄を返す
ガードを追加しました。

- 修正前: `=IFERROR(MATCH(LARGE(入力!$L$9:$L$58,ROWS($AA$2:AA2)),入力!$L$9:$L$58,0),"")`
- 修正後: `=IF(ROWS($AA$2:AA2)>COUNTIF(入力!$L$9:$L$58,">-9.9E+98"),"",IFERROR(MATCH(...),""))`

### 2. 不完全・不正な入力行でダッシュボード全体が崩壊
- 名前だけ入力すると優先キー L/M 列が `""+数値` の #VALUE! となり、LARGE が
  全順位でエラー → ①と④が全滅していました。
- C/D 列に 0 やテキスト（全角数字など）を入力した場合も #DIV/0! / #VALUE! が
  同様に連鎖していました。
- 対策: E 列を `ISNUMBER` ガード付きに変更し、下流（F/I/J/K/L/M）は
  「E が空または 0」の間は空欄／番兵値 -1E+99 を返すよう統一。
  行が完成するまでその人は集計から除外され、他の人の表示は崩れません。
- 改善分類 J / コメント K 列は `"">0` のテキスト比較が TRUE になり
  「要改善（両方遅い）」と誤表示 → 行が完成するまで空欄を返すよう変更。
- 集計用の実績列（B/C）は空セル INDEX の 0 が③マトリクスの原点に点を打つ可能性
  があったため空欄ガードを追加。

### 3. ④コメントが達成状況と矛盾
合計が目標内（例: Dさん -0.03）でも片工程が遅いと「ピック導線を改善」と表示
されていました。合計達成を優先する判定に変更:

- 両工程とも遅い → 差 +0.2 分以上「両工程を見直し」/ 未満「両工程を微改善」
- 合計超過でピッキングが遅い → 「ピック導線を改善」
- 合計超過で梱包が遅い → 「梱包手順を改善」
- 合計達成・両工程とも目標内 → 「良好」/ それ以外 → 「目標達成」

### 4. グラフ①②が 50 枠固定で、少人数だとバーが端に密集
OFFSET＋COUNT の可変定義名（集計用シートスコープ）を追加し、①②の系列参照を
差し替え。入力した人数分だけ表示されるようになりました（③は従来どおり全 50
系列を持ちますが、空系列は描画されません）。

### 5. 細部の仕上げ
- ④の「目標との差」の色分けを `<0` → `≤0` に修正（±0.00 は達成扱いで青に）
- ④の空行の赤チップ・罫線を非表示化し、達成者の順位チップを緑に
  （条件付き書式で入力行のみ罫線・チップを描画）
- ダッシュボードの目盛線を非表示に
- 目標値（入力!B5/C5）を変更するとダッシュボードの見出し・注記・②の凡例
  「ピッキング目標 x.x分」も追従するよう数式化
- 使い方シートの会話調の文言を整理

## 検証
- 全 XML パートの well-formed 検証、ZIP 構造検証
- `formulas` ライブラリ（Excel 互換計算エンジン）で全 1,866 式セルを再計算し、
  保存キャッシュと完全一致（不一致 0・エラー 0）
- シナリオテスト: 名前のみ入力 / 0 入力 / 全角テキスト入力 / 9 人目追加
  （合計タイあり）/ 全員クリア — いずれもエラーなく期待どおりの表示
- 独立エージェントによる OOXML 構造・チャート参照・数式ロジックの敵対的監査
  （確定した欠陥 0 件）
